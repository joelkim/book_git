apiVersion: v1
kind: Service
metadata:
  name: {{ include "book-git.fullname" . }}
  labels:
    {{- include "book-git.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "book-git.selectorLabels" . | nindent 4 }}
